{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field%}
    <a href="{{ admin.generateUrl('edit', {'id' : object.id}) }}">
        {% thumbnail value, 'admin' with {'width': 100, 'height': 80} %}
    </a>
{% endblock %}